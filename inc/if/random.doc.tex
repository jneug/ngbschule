\subsection{Dokumentation der Klasse \code{Random}}\label{subsec:doc-random}

\begin{klassenDokumentation}
	\methodenDokumentation{Konstruktor}{\code{Random()}}{
		Erstellt einen neuen Zufallsgenerator.
	}
	\methodenDokumentation{Anfrage}{\code{int nextInt()}}{
		Erzeugt eine neue Zufallszahl vom Typ int.
	}
	\methodenDokumentation{Anfrage}{\code{int nextInt(int pGrenze)}}{
		Erzeugt eine neue Zufallszahl vom Typ int zwischen 0 und \code{pGrenze-1}.
	}
	\methodenDokumentation{Anfrage}{\code{boolean nextBoolean()}}{
		Erzeugt einen zufälligen Wahrheitswert, der zu \prozent{50} \code{true} und zu \prozent{50} \code{false} ist.
	}
	\methodenDokumentation{Anfrage}{\code{double nextDouble()}}{
		Erzeugt eine neue Zufallszahl vom Typ double zwischen 0 und 1.
	}
\end{klassenDokumentation}