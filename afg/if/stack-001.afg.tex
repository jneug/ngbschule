Unten sehen sie \emph{Struktogramme} der Operationen \code{push} und \code{pop}. Studieren sie den Aufbau und erklären sie das \emph{Verkettungsprinzip} des Datentyps Stapel anhand eines Beispiels.

\subsubsection*{Pop-Operation}
\nss{stack.push}

\subsubsection*{Push-Operation}
\nss{stack.pop}