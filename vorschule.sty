
%% -----------------
%%
%% Eigene Erweiterungen zum 'schule' Paket
%%
%% -----------------
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{vorschule}[2018/11/28  Paket mit eigenen Erweiterungen, die vor dem schule-Paket geladen werden müssen]


% TODO: Kann auch über globale Optionen in der Dokumentklasse gelöst werden!

% Optionen an Pakete weitergeben, die von schule geladen werden
\PassOptionsToPackage{ngerman, outer}{struktex}
\PassOptionsToPackage{skins}{tcolorbox}
\PassOptionsToPackage{pdftex,hidelinks,bookmarks,bookmarksnumbered}{hyperref}
\PassOptionsToPackage{
	zitate=quotes,
	loesungen=keine,
	seitenzahlen=auto
}{schule}

% http://tex.stackexchange.com/questions/26490/latex-datatool-problems
\RequirePackage{bookmark}

\RequirePackage{qrcode}

%% Jobanmen aufteilen
%% Trennen bei "--"
\newcommand{\jobbasename}{\jobname}
\newcommand{\jobsuffix}{}
\newcommand{\dateinamenTeilen}{%
  \def\split##1--##2!!!{\def\jobbasename{##1}\def\jobsuffix{##2}}%
  \expandafter\split\jobname!!!%
}